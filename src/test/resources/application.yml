spring:
  main:
    banner-mode: "off"
  datasource:
    data: classpath:test-data.sql
    schema: classpath:test-schema.sql
    initialization-mode: embedded
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: false
        format_sql: true
