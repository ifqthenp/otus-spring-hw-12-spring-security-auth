spring:
  main:
    banner-mode: "off"
  datasource:
    data: test-data.sql
    schema: test-schema.sql
    initialization-mode: embedded
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: false
        format_sql: true
